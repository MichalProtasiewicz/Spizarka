import React from 'react';
import { storiesOf } from '@storybook/react';
import ShoppingAlert from './ShoppingAlert';

storiesOf('Molecules/ShoppingAlert', module).add('ShoppingAlert', () => (
  <ShoppingAlert/>
));
